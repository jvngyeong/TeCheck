<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
   "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="DDL.mapper.GoodsMapper">
	<sql id = "goodsColumns">
		GOODS_NUM, GOODS_NAME, GOODS_PRICE, GOODS_CONTENTS, VISIT_COUNT, GOODS_MAIN_IMAGE, GOODS_DETAIL_IMAGE, GOODS_MAIN_STORE_IMAGE, GOODS_DETAIL_STORE_IMAGE, GOODS_REGIST, GOODS_UPDATE_DATE
	</sql>
	
	<insert id="goodsWrite" parameterType="goodsDTO">
		insert into goods(<include refid="goodsColumns"></include>)
		values (#{goodsNum},#{goodsName},#{goodsPrice},#{goodsContents}
				,0,#{goodsMainImage},#{goodsDetailImage}
				,#{goodsMainStoreImage},#{goodsDetailStoreImage}
				,sysdate ,sysdate)
	</insert>
	
	<select id="goodsListSelect">
		select <include refid="goodsColumns"></include> from goods
	</select>
	
	<select id = "goodsSelectOne">
		select <include refid="goodsColumns"></include> from goods where goods_num = #{goodsNum}
	</select>
	<update id= "goodsUpdate">
		update goods set goods_name = #{goodsName},goods_price = #{goodsPrice}
						,goods_contents = #{goodsContents},visit_Count = #{visitCount},
						goods_Main_Image = null,goods_Detail_Image = null,
						goods_Main_Store_Image = null,goods_Detail_Store_Image = null,
						goods_regist = #{goodsRegist}, goods_Update_Date = sysdate where goods_num = #{goodsNum}			
	</update>
	<delete id= "goodsDelete">
		delete from goods where goods_num = #{goodsNum}
	</delete>
	 
</mapper>